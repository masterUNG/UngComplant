import 'package:flutter/material.dart';

class WidgetCheckList extends StatelessWidget {
  const WidgetCheckList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}